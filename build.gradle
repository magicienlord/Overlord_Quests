plugins {
    id 'net.neoforged.moddev.legacyforge' version '2.0.137' apply false
    id 'org.moddedmc.wiki.toolkit' version '0.4.1'
}

wiki {
    wikiAccessToken = System.getenv("WIKI_ACCESS_TOKEN")
    docs.create("questlog") {
        root = file("docs/")
    }
}
